version: '3.9'
services:
  backend:
    build: ./backend
    ports: 
      - "5000:5000"
    container_name: OSINT_backend
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    container_name: OSINT_frontend
